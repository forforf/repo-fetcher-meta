{
  "name": "repo-fetcher-meta",
  "version": "0.0.2",
  "author": "Dave Martin <dpmartin@yahoo.com>",
  "description": "Injects metadata into repo object if a .repo-meta.yml exists",
  "repository": {
    "type": "git",
    "url": "https://github.com/forforf/repo-fetcher-meta.git"
  },
  "dependencies": {},
  "devDependencies": {
    "karma": "~0.10",
    "karma-jasmine": "~0.1"
  },
  "scripts": {
    "test": "./node_modules/.bin/karma start ./test/karma.config.js --single-run --browsers PhantomJS"
  }
}
